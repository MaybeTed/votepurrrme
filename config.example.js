const config = {
	// username: 'USERNAME',
	// password: 'PASSWORD',
	GOOGLE_CLIENT_ID: 'Put client id here',
	GOOGLE_CLIENT_SECRET: 'put client secret here' 
};

// change name of file from config.example.js to config.js

module.exports = config;
